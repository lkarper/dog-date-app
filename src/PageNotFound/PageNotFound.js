import React from 'react';

const PageNotFound = (props) => {
    return (
        <section className='section'>
            <header>
                <h1>Woof...</h1>
            </header>
            <p>We couldn't sniff out the page you're looking for.  Check the address and try again.</p>
        </section>
    );
}

export default PageNotFound;